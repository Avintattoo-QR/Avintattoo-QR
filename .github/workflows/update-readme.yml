name: Auto Update README for AutoFlow QR

on:
  push:
    branches:
      - main
    paths:
      - 'index.html'
      - 'qr-code.png' 
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Extract URL and Update README
        run: |
          # 1. שליפת יעד ההפניה מתוך index.html - מותאם לקוד החדש
          # מחפש את המחרוזת בתוך const targetUrl = "..."
          TARGET_URL=$(grep -oP 'const targetUrl = "\K[^"]+' index.html | head -1)
          
          # אם לא נמצא URL, נשתמש בברירת מחדל כדי לא לשבור את ה-README
          if [ -z "$TARGET_URL" ]; then
            TARGET_URL="https://avintattoo.com/"
          fi

          # ניקוי פרמטרי UTM לתצוגה יפה בטבלה
          DISPLAY_URL=$(echo $TARGET_URL | cut -d'?' -f1)
          
          # 2. הגדרת כתובת האתר החי (GitHub Pages)
          LIVE_SITE_URL="https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
          
          # 3. זמן עדכון אחרון (זמן ישראל המשוער UTC+3)
          LAST_UPDATE=$(date -u -d "+3 hours" +'%d/%m/%Y %H:%M')

          # 4. כתיבת קובץ ה-README.md הממותג
          echo "# 🚀 מערכת QR דינמית - AutoFlow" > README.md
          echo "" >> README.md
          echo "ניהול הפניות חכמות ואוטומציה עסקית מבית **AutoFlow**. מאגר זה מאפשר לשנות את יעד הסריקה של ה-QR באופן מיידי דרך שינוי הקישור ב-index.html." >> README.md
          echo "" >> README.md
          echo "## 🤳 קוד ה-QR לסריקה" >> README.md
          echo "זהו הקוד הקבוע להדפסה. ניתן לעדכן את הקישור בקוד ה-HTML מבלי להחליף את ה-QR הפיזי." >> README.md
          echo '<p align="center">' >> README.md
          echo '  <img src="./qr-code.png" width="300" style="border: 10px solid white; border-radius: 10px;" alt="AutoFlow QR Code">' >> README.md
          echo '</p>' >> README.md
          echo "" >> README.md
          echo "---" >> README.md
          echo "" >> README.md
          echo "## 📊 סטטוס מערכת" >> README.md
          echo "| נתון | פרטים |" >> README.md
          echo "| :--- | :--- |" >> README.md
          echo "| **סטטוס** | פעיל ותקין ✅ |" >> README.md
          echo "| **יעד הפניה נוכחי** | [$DISPLAY_URL]($TARGET_URL) |" >> README.md
          echo "| **דף נחיתה פעיל (Live)** | [$LIVE_SITE_URL]($LIVE_SITE_URL) |" >> README.md
          echo "| **עדכון אחרון** | $LAST_UPDATE |" >> README.md
          echo "| **ניטור** | Google Analytics 4 (G-4VPH6LG668) 📈 |" >> README.md
          echo "" >> README.md
          echo "---" >> README.md
          echo '<p align="right"><b>Developed by Almog787 </b></p>' >> README.md

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "Bot: Updated AutoFlow README status with current redirect" || echo "No changes to commit"
          git push
