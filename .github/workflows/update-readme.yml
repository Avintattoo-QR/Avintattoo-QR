name: Auto Update README with Avintattoo QR

on:
  push:
    branches:
      - main
    paths:
      - 'index.html'
      - 'Avintattoo-QR.png' # הבוט ירוץ אם תשנה את הקישור או את התמונה
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Extract URL and Update README
        run: |
          # 1. שליפת יעד ההפניה מתוך index.html
          TARGET_URL=$(grep -oP 'const targetUrl = "\K[^"]+' index.html)
          DISPLAY_URL=$(echo $TARGET_URL | cut -d'?' -f1)
          
          # 2. זמן עדכון אחרון
          LAST_UPDATE=$(date +'%d/%m/%Y %H:%M')

          # 3. כתיבת קובץ ה-README.md
          echo "# 📲 מערכת QR דינמית - Avintattoo" > README.md
          echo "" >> README.md
          echo "### 🤳 קוד ה-QR לסריקה:" >> README.md
          # שימוש בשם הקובץ המדויק שלך לתצוגה
          echo "![Avintattoo QR](./Avintattoo-QR.png)" >> README.md
          echo "" >> README.md
          echo "---" >> README.md
          echo "### ℹ️ פרטי הפניה" >> README.md
          echo "- **סטטוס:** פעיל ✅" >> README.md
          echo "- **יעד נוכחי:** [$DISPLAY_URL]($DISPLAY_URL)" >> README.md
          echo "- **עדכון אחרון:** $LAST_UPDATE" >> README.md
          echo "" >> README.md
          echo "---" >> README.md
          

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "Bot: Updated README with Avintattoo-QR.png" || echo "No changes to commit"
          git push
